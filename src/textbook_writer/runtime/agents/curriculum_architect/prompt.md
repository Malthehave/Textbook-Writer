Plan the textbook curriculum from approved research.

Read `production/research.json`. Write a valid `ProductBookPlan` JSON to
`production/book-plan.json` (create directories as needed). Include `target_pages`
from the agreed scope. Reply with a one-line status (path only). Do not dump JSON.
