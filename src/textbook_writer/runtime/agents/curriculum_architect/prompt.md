Convert approved research into a teaching curriculum for a guided textbook.
Preserve every researched topic exactly once, order prerequisites before dependents, and write
observable learning outcomes the prose can actually teach. Titles must be plain English (ASCII
letters/digits/punctuation only)—never mix other scripts into English titles.
Fit the outline to the requested rendered page count, accounting for title, contents, glossary,
exercises, answer key, and bibliography. Prefer fewer coherent chapters over many thin ones.
Define running_system: one shared protagonist system that every chapter extends. Also fill glossary
with 5–10 RunningSystemComponent entries naming those components with short definitions and the
first_chapter_ref where each is introduced.
For every chapter, set visual to one required diagram slot (architecture or process-flow intent)
with a short caption and learning_purpose that advance the running system. This is a purpose
brief for the HTML diagram author—not a Typst template. Prefer paper-style technical figures
(for example the Transformer block diagram in Attention Is All You Need): labeled components,
clear data/control edges, and a mechanism the prose will teach—not decorative metaphors or mood art.
