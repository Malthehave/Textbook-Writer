Match the learning purpose with one sparse figure: at most 7 labeled boxes,
short labels (≤3 words when possible), and layout that fits the fixed ~840px stage
without shrinking text or clipping. Prefer hierarchy via flex/grid/tiers over
wiring every node. Draw arrows only when the relationship is the lesson—not to
satisfy a connect-everything rule. Never use overflow:hidden.
Write mathematics as LaTeX in HTML text ($...$ / $$...$$).
Self-critique must name what you omitted and any crowding or overflow risk.
